'use client';

import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Bot } from 'lucide-react';

declare global {
  namespace JSX {
    interface IntrinsicElements {
      'gradio-app': React.DetailedHTMLProps<React.HTMLAttributes<HTMLElement> & {
        src?: string;
      }, HTMLElement>;
    }
  }
}

export default function Home() {
  return (
    <main className="flex min-h-screen w-full flex-col items-center justify-center p-4 sm:p-8 relative overflow-hidden font-body bg-background">
      <div 
        className="absolute inset-0 z-0"
        style={{
          backgroundImage: 'radial-gradient(circle at 15% 25%, hsl(var(--primary) / 0.1), transparent 35%), radial-gradient(circle at 85% 75%, hsl(var(--accent) / 0.12), transparent 40%)'
        }} 
      />
      <div 
        className="absolute inset-0 z-0"
        style={{
          backgroundImage: 'linear-gradient(hsl(var(--primary)/0.03) 1px, transparent 1px), linear-gradient(90deg, hsl(var(--primary)/0.03) 1px, transparent 1px)',
          backgroundSize: '2.5rem 2.5rem'
        }} 
      />
      
      <div className="z-10 w-full max-w-4xl mx-auto animate-fade-in-up" style={{ animationDuration: '0.8s' }}>
        <div className="text-center mb-8">
          <h1 className="font-headline text-4xl sm:text-5xl md:text-6xl font-bold tracking-tight text-foreground">
            AI Image Detective
          </h1>
          <p className="mt-4 text-lg sm:text-xl text-foreground/80 max-w-2xl mx-auto">
            Instantly analyze if an image was generated by AI using the embedded model below.
          </p>
        </div>

        <Card className="w-full bg-card/80 backdrop-blur-sm border-border/50 overflow-hidden">
            <gradio-app src="https://thrimurthi2025-ai-or-not.hf.space"></gradio-app>
        </Card>
      </div>

      <footer className="absolute bottom-4 text-sm text-foreground/60 z-10 animate-fade-in-up" style={{ animationDuration: '0.8s', animationDelay: '0.2s' }}>
        Developed by Diljith A K
      </footer>
    </main>
  );
}
